# Starter - Three.js Interaction, Assets, and Animation

Begin here.

- `index.html` and `styles.css` are finished: the page, its controls, and the Select buttons already work once the JavaScript behind them does.
- `js/loader.js`, `js/exhibit.js`, `js/app.js`, `js/describe.js`, and `js/main.js` have twelve numbered TODOs between them, in that order: TODO 1 (loader.js), TODOs 2-4 (exhibit.js), TODOs 5-7 (app.js), TODO 8 (describe.js), TODOs 9-12 (main.js).
- Until TODO 4 is finished, the fox and milk truck pedestals stay a dimmed placeholder shape: that is expected, not an error.
- Until TODO 8 is finished, the scene description reads an empty string: also expected.

Open everything through a local server (`http://`).

Full instructions: [`../README.md`](../README.md).
