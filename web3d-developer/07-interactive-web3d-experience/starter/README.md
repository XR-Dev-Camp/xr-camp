# Starter — Phase 3 Capstone - Interactive Web3D Experience

Begin here. Read [`brief.md`](brief.md) and [`rubric.md`](rubric.md) first.

- `index.html`, `styles.css`, `attribution.html`, `CHANGELOG.md`, and `js/exhibit.js`, `js/loader.js`, `js/describe.js`, `js/app.js` are finished — you should not need to change them for the required steps.
- `js/main.js` has 4 numbered TODOs (3, 4, 5, 6); `index.html` has 2 (1, 2), tackled first. All six are numbered in the order the walkthrough tackles them.
- No errors are expected before you start: the starter runs, loads, and is fully keyboard-usable already (it is 3.5's model explorer). Before TODO 1-4, selecting an exhibit updates the scene description but the info panel stays empty. Before TODO 2 and 5, the performance budget numbers do not appear.

Open everything through a local server (http://).

Full instructions: [`../README.md`](../README.md).
