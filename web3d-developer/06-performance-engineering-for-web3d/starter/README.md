# Starter — Performance Engineering for Web3D

Begin here.

- `index.html`, `styles.css`, and `js/main.js`, `js/describe.js` are finished — you should not need to change them for the required steps.
- `js/hall.js`, `js/textures.js`, and `js/app.js` contain nine numbered TODOs (1-9), in the order the walkthrough tackles them: 1 and 2-4 in `hall.js`, 5 in `textures.js`, 6-8 in `hall.js`, 9 in `app.js`.
- No errors are expected before you start: the starter runs correctly, just slowly. Each TODO marks something wasteful, not something broken — watch the Stats panel change as you fix each one.

Open everything through a local server (http://).

Full instructions: [`../README.md`](../README.md).
