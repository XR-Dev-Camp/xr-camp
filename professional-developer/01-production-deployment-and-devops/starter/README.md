# Starter - Production Deployment and DevOps

Begin here.

- `app/` is the finished virtual exhibit (Course 3.7). It is complete: do not edit it. It is the thing your pipeline deploys.
- `index.html` and `styles.css` are the release dashboard. TODOs 1-3 are in `index.html`.
- `workflows/deploy.yml` (TODOs 4-9) and `workflows/rollback.yml` (TODOs 10-12) are example GitHub Actions workflows. They only run once copied to `.github/workflows/` at the root of your own repository (see the main README's Setup section).

Before any TODO is done, `index.html` shows an incomplete dashboard, and neither workflow file will do anything useful if copied and run: several of their jobs have no steps yet.

Open everything through a local server (http://).

Full instructions: [`../README.md`](../README.md).
