# Completed - Production Deployment and DevOps

The reference solution. Open it after you have tried the starter.

- `index.html` is the finished release dashboard.
- `workflows/deploy.yml` and `workflows/rollback.yml` are the finished example workflows, ready to copy to `.github/workflows/` at the root of your own repository.
- `app/` is the same virtual exhibit as `starter/app/`, unchanged, included here so this folder is self-contained.

Open everything through a local server (http://).

Full instructions: [`../README.md`](../README.md).
