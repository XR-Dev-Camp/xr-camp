# Completed - Shapes and Colour

The reference solution. Open it after you have tried the starter.

- `index.html` is the finished scene: three coloured shapes, a real scene description, a keyboard-reachable highlight button, and a reduced-motion-aware rotation with its own Pause animation button.
- `script.js` holds all of the interaction; the HTML has no inline scripts.

Open through a local server (http://).

Full instructions: [`../README.md`](../README.md).
