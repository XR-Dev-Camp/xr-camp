# The code under review, after Ana's review

The session planner from Course 2.3, with the two changes Ana made after her
AI-assisted review. Every other line is exactly as it was.

- `js/main.js`: an empty topic is now also announced through the status
  region (review item 7).
- `check.html`: two new checks, for 59 and 60 minutes (review item 6).

Search for `Review item` to find both changes.

Open everything through a local server (http://).
