# The code under review: the session planner

A copy of the reference session planner from Course 2.3
(`frontend-engineer/03-application-architecture-and-maintainable-code/completed`).
It is the code you review with an AI assistant in this lesson.

If you finished your own planner in Course 2.3, review yours instead: it is
your code, so you have the right to share it, and you know what it should do.

- `index.html`: the planner page.
- `js/`: `config.js`, `utils.js`, `store.js`, `components/`, and `main.js`.
- `check.html`: the checks for `utils.js`.

Make a Git commit before you change anything, and one after each change you
accept from the review.

Open everything through a local server (http://).
