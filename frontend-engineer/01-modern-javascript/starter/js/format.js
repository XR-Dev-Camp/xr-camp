// format.js: small, pure functions that turn numbers into words.

// TODO 2: Export an arrow function hours(minutes) that returns the number of
// hours, rounded. Test it in the console after TODO 7.

// TODO 3: Export plural(count, one, many), where many has a DEFAULT value of
// one + 's'. plural(1, 'hour') is 'hour'; plural(3, 'hour') is 'hours'.

// TODO 4: Export describeTime, which DESTRUCTURES { minutes, sessions } from
// the object it is given, and returns text like "12 hours · 16 sessions".
