// data.js: loading the course data.

// TODO 5: Export an async function loadCatalog(url = 'data/catalog.json').
// await fetch(url); if response.ok is false, throw a new Error with a clear
// message that includes response.status; otherwise return response.json().

// TODO 6: Export groupByPhase(lessons), which uses reduce to build an object
// like { 0: [lessons...], 1: [lessons...] }.

// TODO 8: Export totals(lessons), which uses reduce to add up minutes,
// sessions, and how many lessons have status 'ready'.
