# Changelog

Every notable change to My XR Camp, for the people who use it, in plain
words. Version numbers follow [Semantic Versioning](https://semver.org/):
MAJOR.MINOR.PATCH. Keep `js/config.js`'s `APP_VERSION`, `sw.js`'s `VERSION`,
and this file's newest heading equal to each other; `check.html` checks that
`js/config.js` and `sw.js` agree.

<!-- TODO (release, Course 2.7): write your 1.0.0 entry below. List what My
     XR Camp does, in plain words, grouped under "### Added". Compare with
     completed/CHANGELOG.md only after you have written your own. -->

## 1.0.0 — YYYY-MM-DD

### Added

-
