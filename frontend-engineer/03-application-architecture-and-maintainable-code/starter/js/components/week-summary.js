// week-summary.js: the progress sentence under the lists.

import { SESSION_MINUTES, WEEKLY_GOAL } from '../config.js';
import { describeMinutes } from '../utils.js';

// TODO 10 (continued): Export weekSummary(sessions), which returns an ARRAY
// of elements: a <p> saying "2 of 4 sessions done this week (1 hour
// 30 minutes)."; plus, when the goal is reached, a second
// <p class="goal-reached">Weekly goal reached. Well done!</p>.
// No 45 and no 4 in this file: use the constants.
