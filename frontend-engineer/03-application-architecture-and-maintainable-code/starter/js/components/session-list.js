// session-list.js: fill one <ul> with sessions, or say it is empty.

import { sessionItem } from './session-item.js';

// TODO 10: Export renderSessionList(list, sessions). If there are no
// sessions, put one <li class="empty"> saying "Nothing here yet.";
// otherwise replaceChildren with a sessionItem for each.
