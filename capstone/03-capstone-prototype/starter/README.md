# Starter - Capstone Prototype

Begin here.

- `prototype-plan.md`, `test-log.md`, and `design-review.md` are Markdown templates with 7 numbered TODOs between them (1–3 in `prototype-plan.md`, 4–5 in `test-log.md`, 6–7 in `design-review.md`).
- `index.html` and `js/main.js` are a minimal three.js page with 4 numbered TODOs in `js/main.js`. Before TODO 1 is complete, the canvas stays blank on purpose — this is expected, not an error.
- `rubric.md` tells you what each level of quality looks like.

Open everything through a local server (`http://`); modules need it.

Full instructions: [`../README.md`](../README.md).
