# Starter - Spatial UX Design

Begin here. Follow the walkthrough in [../README.md](../README.md).

- Finished, given to you: `index.html`, `styles.css`, `data/catalog.json`, `js/app.js`, `js/data.js`, `js/xr.js`. You do not need to change these.
- Ten numbered TODOs, in this order: `js/panels.js` (TODOs 1–3), `js/layout.js` (TODOs 4–6), `js/locomotion.js` (TODOs 7–8), `js/describe.js` (TODO 9), `js/main.js` (TODO 10).
- Before TODO 1, the room already renders: a plain grey plaque stands in for each panel, teleporting jumps instantly with no fade, and the scene description reads "Scene description coming soon." Nothing throws an error; each TODO replaces one placeholder with the real thing.
- `design-rationale.md` is not code: fill it in as you go, alongside the walkthrough's final step.

Open everything through a local server (`http://`).

Full instructions: [`../README.md`](../README.md).
