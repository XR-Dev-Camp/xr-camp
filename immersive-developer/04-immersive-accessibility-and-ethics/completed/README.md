# Completed - Immersive Accessibility and Ethics

The reference solution. Compare against your own work only after you have
attempted the starter.

- Every problem in [`../starter/`](../starter/) is fixed here. See
  [`audit.md`](audit.md) for the full mapping from each fix to the exact
  XAUR user need or WCAG 2.2 success criterion it answers - and the three
  fixes that neither document covers at all.
- `js/xr.js` is identical to the starter's: entering VR was never part of
  the audit.

Open everything through a local server (`http://`).

Full instructions: [`../README.md`](../README.md).
