# Starter — Spatial Audio, Media, and Presence

Begin here.

- `index.html` and `styles.css` are finished: the page, the audio guide player, the caption paragraph, the transcript list, and every control panel are already built.
- `js/exhibit.js` and `js/xr.js` are finished, carried over from 4.1 (WebXR Foundations).
- `js/app.js`, `js/audio.js`, `js/video.js`, `js/captions.js`, `js/describe.js`, and `js/main.js` each have numbered TODOs (1–10, in that order across the files) for you to complete.
- Before TODO 1, the page loads with no console errors, but pressing "Start pedestal sounds" or reading the scene description will not yet reflect the new audio and media layer.

Open everything through a local server (`http://`), never by double-clicking the file.

Full instructions: [`../README.md`](../README.md).
