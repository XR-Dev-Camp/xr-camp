# Completed — Spatial Audio, Media, and Presence

The reference solution. Compare against your own work only after you have attempted the starter.

Open `index.html` through a local server (`http://`). All ten TODOs from the starter are filled in: three pedestals each play their own spatial sound (`js/audio.js`), a canvas-driven "story screen" shows a short audio guide's captions (`js/video.js`), those captions and a full transcript come from one shared WebVTT file (`js/captions.js`, `assets/captions.vtt`), and the scene description (`js/describe.js`) reports all of it in words.

Full instructions: [`../README.md`](../README.md).
