# Completed - AI for Spatial Computing

Reference solution. Compare against your own work only after you have attempted the project.

Run it with `node server/server.js` from this folder, then open `http://127.0.0.1:8886/`. Run `node --test` inside `server/` to run the automated tests (against the default mock AI provider — no network access or API key needed). See [`../README.md`](../README.md) for the full walkthrough, and this folder's own `server/ai.js`, `server/db.js`, `server/routes.js`, and `js/main.js` for the finished code.
