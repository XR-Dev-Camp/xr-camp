# Completed - Databases and Spatial Application Data

Reference solution. Compare against your own work only after you have attempted the project.

Run it with `node server/server.js` from this folder, then open `http://127.0.0.1:8879/`. Run `node --test` inside `server/` to run the automated tests. See [`../README.md`](../README.md) for the full walkthrough, and this folder's own `server/db.js`, `server/migrations/`, `server/routes.js`, and `js/main.js` for the finished code.
